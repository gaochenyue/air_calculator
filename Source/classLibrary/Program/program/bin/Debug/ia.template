for(i=0,k=0;i<100&&input[j]!=']'&&input[j]!=')';j++)
	{
		if(input[j]>=48&&input[j]<=57)
		{
			temp_str[k]=input[j];
			k++;
		}
		if(input[j]==','||input[j]==';')
		{
			temp_str[k]='\0';
			temp_ia[i]=atoi(temp_str);
			i++;
        	k=0;
		}
	}
	temp_str[k]='\0';
	temp_ia[i]=atof(temp_str);
	j++;